{
  "name": "presh",
  "version": "1.1.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "watchify test -o test/index.browser.js -d",
    "watchExample": "watchify example -o example/index.browser.js -d"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "deep-equal": "^1.0.0",
    "same-value": "^1.0.0"
  },
  "dependencies": {
    "flat-merge": "^1.0.0",
    "string-template": "^0.2.1"
  }
}
